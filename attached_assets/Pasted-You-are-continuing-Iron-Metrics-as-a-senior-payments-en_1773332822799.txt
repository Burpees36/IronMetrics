You are continuing Iron Metrics as a senior payments engineer, QA engineer, and security-minded SaaS architect.

The Billing UI is now in a strong place visually and functionally. The next task is to make billing trustworthy, auditable, permission-safe, and testable.

Current state:
- Billing command center exists
- Member billing tab exists
- Subscription actions exist
- Payment/refund history exists
- Auth protection exists
- E2E browser auth is limited by Replit OAuth

Your task now is to harden the billing system for production trust.

==================================================
PRIMARY OBJECTIVE
==================================================

Implement billing verification, reconciliation, authorization, audit logging, and automated backend testing so the billing system is reliable enough for real-world gym operations.

==================================================
PHASE 1 — WEBHOOK RECONCILIATION AUDIT
==================================================

Audit and harden the Stripe webhook system.

Requirements:
- verify signature validation is correct
- ensure handlers are idempotent
- prevent duplicate payments/subscriptions/refunds from duplicate Stripe events
- log every webhook event and processing result
- capture processing errors in a diagnosable way
- support replay/reprocessing if architecture allows
- confirm subscription/payment/refund state is driven by Stripe-confirmed events where appropriate

Verify handling for at least:
- customer.subscription.created
- customer.subscription.updated
- customer.subscription.deleted
- invoice.created
- invoice.finalized
- invoice.payment_succeeded
- invoice.payment_failed
- charge.refunded
- payment_method.attached

==================================================
PHASE 2 — BILLING SOURCE-OF-TRUTH AUDIT
==================================================

Build a billing audit script and supporting checks that validate the Billing page metrics against actual persisted records.

Validate:
- MRR
- active subscriptions count
- ARM
- failed payments count
- collections this month
- refunds this month
- cancelled members count
- lost revenue estimates

Requirements:
- centralize metric definitions
- remove any remaining inconsistent or duplicated logic
- ensure month/date window logic is correct everywhere
- ensure summary cards match underlying tables and member billing records

Create a developer script that prints a canonical billing truth report for the demo gym.

==================================================
PHASE 3 — ROLE-BASED AUTHORIZATION FOR BILLING
==================================================

Implement and enforce real RBAC for billing actions.

At minimum:
- gym_owner/admin: full billing permissions
- front_desk: limited visibility and limited charge actions if appropriate
- coach: no destructive billing actions
- analyst: read-only billing visibility by default

Enforce this:
- in backend route/service authorization
- in frontend action availability
- without relying only on hidden buttons

Protect:
- refunds
- plan creation/editing
- subscription cancellation
- one-time charges
- billing settings changes

==================================================
PHASE 4 — AUDIT LOGGING
==================================================

Add audit logs for all important billing mutations.

Log:
- actor user id
- gym id
- member id if applicable
- action type
- before/after values where practical
- amount/currency if money-related
- reason entered by user
- timestamp
- source (UI, webhook, system)

At minimum log:
- subscription created
- plan changed
- subscription paused/resumed/cancelled
- one-time charge created
- refund issued
- payment method updated
- webhook reconciliation changes

Expose an admin-friendly audit view or diagnostic endpoint if reasonable.

==================================================
PHASE 5 — BILLING TESTING
==================================================

Add automated tests focused on billing.

Implement:
- unit/integration tests for billing service logic
- API tests for billing routes
- webhook handler tests
- idempotency tests
- duplicate subscription prevention tests
- refund tests
- RBAC tests for billing-sensitive actions
- metric consistency tests for billing summary cards

If full browser E2E remains blocked by OAuth, prioritize backend/API reliability tests.

Add package scripts:
- test:billing
- test:webhooks
- test:rbac
- audit:billing

==================================================
PHASE 6 — FAILURE / RECOVERY UX
==================================================

Improve trust around failure states.

Requirements:
- clear failed payment status on member billing tab
- clear failed payment status in billing dashboard
- clear error messaging for failed mutations
- loading/disabled states for billing actions
- confirmation dialogs for refunds/cancellations
- no misleading success state before backend confirmation

==================================================
DELIVERABLES
==================================================

Actually implement:
- webhook hardening
- billing truth audit
- RBAC for billing
- billing audit logs
- backend/API billing tests
- billing metric consistency checks

Then provide:
1. files changed
2. routes/services hardened
3. tests added
4. package scripts added
5. billing metrics definitions unified
6. remaining risks/setup notes