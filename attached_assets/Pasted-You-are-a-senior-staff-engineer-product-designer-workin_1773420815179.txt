You are a senior staff engineer + product designer working inside Iron Metrics. I need you to upgrade the Schedule page into a production-grade scheduling system that feels polished, trustworthy, and clearly better than Wodify for both gym owners/staff and members.

IMPORTANT CONTEXT
Iron Metrics is a premium gym operating system for CrossFit gyms, not a generic calendar. The UI should feel calm, sharp, premium, and operationally useful. No clutter. Every action should either save time, reduce errors, improve class fill, improve member experience, or improve staff coordination.

CURRENT STATE
The schedule page already has:
- Weekly calendar grid (Mon-Sun, 5 AM - 10 PM)
- Prev/next week navigation + Today button
- Create class dialog
- Click class to open detail sheet
- Coach assignment dropdown
- Check-in flow
- Copy last week
- Templates (save/apply/manage)
- Overlap stacking
- Live “now” line
- Color-coded class types

CURRENT PROBLEMS TO FIX
1. No way to edit a class/event after creation
2. Template contents are not clearly ordered/grouped, making them hard to trust
3. When applying a template, user cannot deselect individual classes before applying
4. Schedule currently has no role-based access control differentiation
5. UX is still “app works” quality, not “gym owner is relieved and impressed” quality
6. Current experience is too staff-centric in some places and not yet ready for member-facing use
7. Need to support gym owners, admins, coaches, front desk, and members using the same schedule system with the correct permissions and experience
8. Need to avoid feature bloat while making it feel top-tier and production ready

PRODUCT GOAL
Build one schedule/calendar system with role-aware behavior:
- Owners/Admins: full scheduling control
- Coaches/Front Desk: operational actions they actually need
- Members: browse, reserve, join waitlist, cancel self, view roster/coaches/workout info as allowed
This must feel like a premium operating system for gyms.

DESIGN PRINCIPLES
- Premium dark UI already exists; preserve and improve it
- Better hierarchy, spacing, grouping, and empty/loading states
- Reduce ambiguity and increase trust
- Fewer clicks for common actions
- Clear permission boundaries
- No ugly admin-only feeling; this should still feel welcoming for members
- Every modal/sheet should have a clear primary action, clear destructive action handling, and obvious status feedback
- Mobile and tablet behavior must be considered, even if desktop is primary

IMPLEMENTATION SCOPE

PART 1 — ROLE-BASED ACCESS CONTROL (REQUIRED)
Add real RBAC to backend routes and frontend rendering.

Target permissions:
- Owner:
  - view all schedule data
  - create/edit/delete classes
  - assign coaches
  - check in members
  - copy week
  - save/apply/edit/delete templates
  - override conflicts
  - manage waitlists and attendance states
- Admin:
  - same as owner except any explicitly owner-only operations if needed
- Coach:
  - view schedule
  - view class rosters
  - check in members
  - mark attendance states
  - view workouts/coaches/member count
  - cannot create/delete classes unless a deliberate permission flag exists
  - cannot manage templates by default
- Front Desk:
  - view schedule
  - check in members
  - manage roster/waitlist
  - maybe create one-off classes only if permission flag exists, otherwise no
  - no template management by default
- Member:
  - view public/member-visible schedule
  - reserve/self-book into eligible classes
  - cancel own reservation within rules
  - join waitlist
  - see coach, spots left, class type, workout summary if available
  - cannot edit schedule or see staff controls
  - cannot check in other members

Implement route middleware similar to how stronger route RBAC is handled elsewhere in the app. Hide buttons and actions in the frontend based on role, but enforce on backend regardless.

PART 2 — EDIT CLASS / EVENT SYSTEM (REQUIRED)
Add full edit functionality for existing classes/events.

Requirements:
- Edit from class detail sheet via clear “Edit Class” CTA
- Open edit modal/sheet prefilled with current values
- Editable fields:
  - name
  - type
  - date
  - start/end time
  - coach
  - capacity
  - description
  - member-visible notes
  - internal staff notes (staff only)
  - visibility / bookable toggle
  - waitlist enabled toggle
  - cancellation window
- Add “save changes” with optimistic UI or crisp refresh
- Add “save + apply to series” when applicable if class is part of a repeated set
- If the class has reservations/check-ins already:
  - warn clearly before changing time/capacity/type
  - show impact summary (“11 members enrolled; changing time will notify affected members”)
- Add duplication action:
  - “Duplicate Class”
- Add safer destructive handling:
  - delete confirmation with consequences
  - if enrolled members exist, show stronger warning and optional notification workflow placeholder

PART 3 — TEMPLATE SYSTEM REBUILD (REQUIRED)
Templates need to feel ordered, readable, and trustworthy.

Requirements:
- Save templates with metadata:
  - template name
  - created by
  - created date
  - total classes
  - total hours scheduled
  - days covered
- Store and display template contents in deterministic order:
  1. sort by weekday (Mon → Sun)
  2. then start time ascending
  3. then class name
- In template management, group by day with sticky day headers or strong day separators
- Each row should show:
  - class name
  - type badge
  - time
  - coach
  - capacity
  - recurrence/source marker if useful
- Add search/filter within template
- Add duplicate template action
- Add rename template
- Add archive/delete template
- Add “last used” and “used X times” if easy to support

PART 4 — APPLY TEMPLATE FLOW WITH SELECTIVE DESELECTION (REQUIRED)
Rebuild template application UX.

Requirements:
- Applying a template must open a proper review screen/modal
- Show classes in sorted/grouped order by day
- Every class has a checkbox selected by default
- User can:
  - deselect individual classes
  - select/deselect all
  - select/deselect by day
- Show summary counts live:
  - selected classes
  - skipped duplicates
  - conflicts
  - net new classes to create
- Conflict handling:
  - identify exact collisions (same day/time or duplicate class signatures)
  - clearly label:
    - New
    - Duplicate / Will Skip
    - Conflict / Needs Review
- Let user choose conflict behavior:
  - skip conflicts
  - create only clean classes
  - optionally replace conflicting empty classes if owner/admin only
- Final CTA text should reflect count:
  - “Apply 23 Classes”
- After apply, show success toast/summary:
  - created
  - skipped
  - conflicted
  - failed

PART 5 — CALENDAR UX POLISH (REQUIRED)
Improve the weekly calendar so it feels premium and high trust.

Calendar improvements:
- Stronger visual hierarchy for current day and current time
- Better class card density and readability
- Improve overlap rendering:
  - cleaner stacking
  - clearer selected state
  - better truncation handling
- Add hover/quick actions for staff on desktop:
  - view
  - edit
  - duplicate
  - delete (where allowed)
- Add better legend/filter bar:
  - class type filters
  - coach filter
  - location/program filter if relevant
  - show only my classes toggle (coach/member context)
- Add occupancy visualization:
  - e.g. 3/12, Full, Waitlist, Low Fill, Nearly Full
- Add optional indicators:
  - workout attached
  - coach assigned / unassigned
  - intro/foundations
  - members on waitlist
- Make empty cells more intentional:
  - subtle affordance for privileged roles
  - not noisy for members
- Improve scroll behavior and sticky hour/day headers
- Ensure keyboard escape, close, focus trap, and accessible interactions in all overlays

PART 6 — MEMBER-FACING SCHEDULE EXPERIENCE (REQUIRED)
Do NOT create a separate disconnected system. Use the same schedule data model with role-aware UI.

Member-facing requirements:
- Members can view schedule in a simplified mode
- On class click, member sees:
  - class name
  - type
  - coach
  - time
  - spots left
  - who’s going / limited roster visibility depending on privacy rules
  - workout summary or “today’s workout”
  - reserve button / join waitlist / cancel reservation
- Show clear status:
  - Book
  - Booked
  - Full
  - Waitlist
  - Checked In
- Member cannot see staff-only controls, internal notes, admin actions, or destructive actions
- If members are allowed to see attendees, implement a privacy-conscious version:
  - initials/avatar/name based on existing product conventions or permissions
- Add self-service cancellation logic placeholder/hooks
- Add no-surprise messaging around booking rules:
  - cancellation cutoff
  - late cancel
  - class full
  - waitlist auto-promotion if implemented

PART 7 — ROSTER / ATTENDANCE UPGRADE (REQUIRED)
Current roster experience needs to feel operationally complete.

Add:
- attendance states:
  - reserved
  - checked in
  - no-show
  - cancelled
  - waitlisted
- roster list should support:
  - search members
  - sort by status / check-in time / name
  - quick actions per member based on role
- show occupancy summary in detail sheet
- allow owner/admin/front desk/coach to manually move member:
  - waitlist → booked
  - booked → checked in
  - booked → no-show
  - checked in → undo if needed
- if capacity is reached, waitlist flow should appear instead of dead-end
- protect all transitions with backend validation

PART 8 — SCHEDULING DATA MODEL / API IMPROVEMENTS (REQUIRED)
Upgrade schema and API only as needed to support production-grade scheduling.

Potential additions:
- class_instances table or equivalent clarity between reusable definition vs actual scheduled occurrence
- schedule_templates + template_items with explicit sort fields
- attendance/reservation status model rather than simple checked-in only logic
- waitlist position
- visibility/bookable flags
- member-facing notes vs internal notes
- audit fields:
  - createdBy
  - updatedBy
  - updatedAt
- soft delete or status where useful
- notification hooks/events for future reminders and schedule change alerts

Maintain tenant isolation and existing architecture conventions.

PART 9 — NOTIFICATIONS / STATUS FEEDBACK HOOKS (LIGHT IMPLEMENTATION)
Do not overbuild full messaging, but prepare the system for it.

Requirements:
- on schedule change affecting enrolled members, create structured hooks/placeholders for:
  - class time changed
  - class cancelled
  - coach changed
- Add user-facing toasts and confirmation messaging
- Add success/error/loading states everywhere
- No silent failures
- All destructive actions must feel safe and explicit

PART 10 — UI/UX DETAILS THAT SHOULD EXIST IN FINAL RESULT
Add or improve:
- empty states
- loading skeletons
- no-results states in filters/search
- better button labels
- better section headers
- capacity warnings
- full/waitlist badges
- safer destructive color usage
- cleaner modals/sheets spacing
- clearer date/time pickers
- stronger ordering and grouping in every list
- better copy in templates and class details
- subtle premium animations only where they add clarity

PART 11 — DELIVERABLES I WANT FROM YOU
Implement the changes directly in the codebase and then provide:

A. FILES CHANGED
For each changed file, explain why it changed.

B. RBAC MATRIX IMPLEMENTED
Show the final permission matrix by role and action.

C. UX IMPROVEMENTS SHIPPED
List the specific UX improvements made.

D. DATA MODEL / API CHANGES
List schema updates, new endpoints, changed endpoints, and validation rules.

E. KNOWN FOLLOW-UPS
List what remains for future sprints, such as notifications, mobile refinement, public booking rules, package/credit integration, recurring schedules, etc.

F. TEST CHECKLIST
Provide a concise manual test checklist covering:
- owner flow
- coach flow
- front desk flow
- member flow
- edit class flow
- template selective apply
- conflict handling
- waitlist flow
- permission enforcement

IMPORTANT IMPLEMENTATION NOTES
- Make design decisions that reduce operational mistakes
- Prefer polished, obvious UX over cleverness
- Do not add bloated enterprise nonsense
- Preserve existing styling language and make it feel more refined
- Reuse existing component patterns where possible
- Keep code clean, maintainable, and production-minded
- If you encounter weak existing structure, refactor as needed instead of hacking around it
- Backend permission enforcement is mandatory
- Frontend role-aware rendering is mandatory
- The result should feel like a gym owner could confidently switch their daily operations onto this page

SUCCESS CRITERIA
The finished schedule page should:
- support real-world gym operations
- support both staff and member use from the same schedule system
- feel materially more polished than before
- prevent permission leaks
- make templates trustworthy and easy to use
- make editing classes obvious and safe
- make applying templates flexible and understandable
- feel like premium gym software, not a rough internal tool